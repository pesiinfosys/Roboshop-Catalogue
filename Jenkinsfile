pipeline {
    agent{
        label 'Agent-1'
    }
    // options {
        
    // }
    // environment {

    // }
    // parameters{
        
    // }
    stages{
        stage('Install Dependencies') {
            steps {
                // sh 'npm install'
                echo "Installing Dependencies"
            }
        }
        stage('Unit Testing') {
            steps {
                echo "Unit Test is done here"
            }
        }
    }
    // post {
        
    // }
}